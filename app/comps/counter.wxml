<view class="component" bindtap="onTap">
  <view class="text-center">Here is the component</view>
  <view class="text-center">{{ count }}</view>
  <view class="ul">
    <view wx:for="{{items}}" class="li">
      # {{item}}
    </view>
  </view>
</view>
